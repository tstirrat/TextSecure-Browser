import Ember from 'ember';
import PopupRouteMixin from 'textsecure-chrome/mixins/popup-route';

module('PopupRouteMixin');

// Replace this with your real tests.
test('it works', function() {
  var PopupRouteMixinObject = Ember.Object.extend(PopupRouteMixin);
  var subject = PopupRouteMixinObject.create();
  ok(subject);
});
